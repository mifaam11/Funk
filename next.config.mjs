/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['https://www.freepik.com/', "images.unsplash.com", "img.freepik.com"], // Replace with your free image domains
    // Replace with your free image domains
  },
};

export default nextConfig;
